import "@testing-library/jest-dom/vitest";

// Shared test setup (JS to avoid TS transpilation needs inside node_modules)
// Add common polyfills / mocks here if required later.

// Set up shared environment variables for testing
// process.env.NODE_ENV = 'test'
